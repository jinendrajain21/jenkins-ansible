pipeline {
  agent any
  options {
     ansiColor('xterm')
  }
  stages {
    stage ('Ansible-Version') {
      steps {
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-playbook -i inventory host.yml
        '''
      }
    }
  }
}
